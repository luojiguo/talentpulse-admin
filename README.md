# TalentPulse Admin 招聘管理系统

TalentPulse 是一个现代化的全栈招聘管理平台，旨在连接求职者与招聘方，提供高效的职位发布、简历投递、以及实时沟通功能。本项目集成了 Google Gemini AI 能力，为用户提供智能化的招聘体验。

## 🚀 主要功能

*   **多角色系统**：支持 求职者 (Candidate)、招聘者 (Recruiter) 和 管理员 (Admin) 三端登录与管理。
*   **职位管理**：招聘者可发布、编辑和管理职位信息。
*   **求职者中心**：完善的个人简历管理、职位浏览与投递流程。
*   **实时沟通**：内置消息中心，支持求职者与招聘者即时聊天。
*   **AI 赋能**：集成 Google GenAI (Gemini)，提供智能辅助功能（如简历优化、智能匹配等）。
*   **企业认证**：完善的企业入驻与资质审核流程。

## 🛠️ 技术栈

### 前端 (Front_End)
*   **框架**: React 19, TypeScript
*   **构建工具**: Vite
*   **UI 组件库**: Ant Design 6.x
*   **图表**: ECharts, Recharts
*   **路由**: React Router v7
*   **其他**: Axios, Lucide React (图标)

### 后端 (backend)
*   **运行环境**: Node.js
*   **框架**: Express
*   **数据库**: PostgreSQL
*   **认证**: JWT, Bcrypt
*   **文件处理**: Multer (上传), PDF-parse
*   **安全性**: Helmet, CORS

## 📂 项目结构

```
talentpulse-admin/
├── Front_End/          # 前端项目源码 (React + Vite)
│   ├── src/            # 核心代码
│   ├── vite.config.ts  # Vite 配置 (端口: 3000)
│   └── ...
├── backend/            # 后端 API 服务 (Node.js + Express)
│   ├── src/            # 业务逻辑 (Controllers, Models, Routes)
│   ├── server.js       # 入口文件 (端口: 3001)
│   └── ...
├── Talent.sql          # 数据库初始化脚本
└── README.md           # 项目说明文档
```

## 🚦 快速开始

### 1. 环境准备
*   Node.js (v16+)
*   PostgreSQL
*   npm 或 pnpm

### 2. 数据库设置
创建 PostgreSQL 数据库并导入初始化脚本：
```bash
# 假设你已经安装了 PostgreSQL
createdb talentpulse
psql -U postgres -d talentpulse -f Talent.sql
```
*注意：数据表结构定义见 `Talent.sql`，测试数据可查看 `mock_data.sql` (如果存在)。*

### 3. 后端启动
进入 `backend` 目录，安装依赖并启动服务：
```bash
cd backend
npm install

# 复制 .env.example 为 .env (如有) 并配置数据库连接和 API Key
# 默认端口: 3001
npm run dev
```

### 4. 前端启动
进入 `Front_End` 目录，安装依赖并启动开发服务器：
```bash
cd Front_End
npm install

# 默认端口: 3000
npm run dev
```

## 📜 常用命令

*   **一键启动** (Windows): 运行根目录下的 `start_project.bat`
*   **前端**:
    *   `npm run dev:admin`: 启动管理端 (Port 3002)
    *   `npm run dev:user`: 启动用户端 (Port 3000)
*   **后端**:
    *   `npm run dev`: 开发模式启动

## 📝 许可证
MIT License
