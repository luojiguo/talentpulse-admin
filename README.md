# TalentPulse Admin 招聘管理系统

TalentPulse 是一个功能强大的全栈招聘管理平台，专为连接求职者与招聘方而设计。本项目采用现代化的技术栈构建，集成了前沿的 AI 能力，旨在提供高效、智能且用户友好的招聘体验。

## 🌟 核心亮点

*   **智能 AI 驱动**: 集成 Google Gemini AI，为求职者提供简历优化建议，为招聘方提供职位描述智能生成与人岗匹配分析。
*   **全流程招聘管理**: 覆盖从职位发布、简历投递、即时沟通到面试安排的全生命周期管理。
*   **多角色权限体系**: 精细化的角色管理，支持求职者、招聘者和管理员三种不同视角的业务逻辑。
*   **实时互动体验**: 内置 WebSocket 消息系统，支持文字、表情及文件传输，让沟通零距离。

---

## 🚀 功能模块详解

### 1. 求职者端 (Candidate)
*   **个人中心**: 简历在线编辑、经历管理、教育背景维护。
*   **职位搜索**: 多维度职位筛选（薪资、地点、职能等）。
*   **智能推荐**: 基于 AI 的职位推荐算法。
*   **消息互动**: 与招聘方在线直聊，查看消息已读/未读状态。

### 2. 招聘者端 (Recruiter)
*   **职位管理**: 职位的发布、编辑、下架及刷新。
*   **候选人管理**: 简历筛选、面试邀请、状态流转（待沟通、已面试、已录用等）。
*   **企业主页**: 维护企业基本信息、上传营业执照进行认证。
*   **数据看板**: 实时查看职位浏览量、投递量等关键指标。

### 3. 管理员端 (Admin)
*   **用户管理**: 比如封禁违规账号。
*   **内容审核**: 审核企业资质、职位内容的合规性。
*   **系统配置**: 维护系统基础参数。

---

## 🛠️ 技术架构

本项目采用前后端分离架构，确保了系统的可扩展性与维护性。

### 前端 (Front_End)
*   **核心框架**: React 19 + TypeScript (强类型约束，提升代码质量)
*   **构建工具**: Vite (极速冷启动与热更新)
*   **UI 组件库**: Ant Design 6.x (企业级中后台组件系统的最佳实践)
*   **状态管理**: React Hooks & Context API
*   **数据可视化**: ECharts / Recharts
*   **路由管理**: React Router v7
*   **网络请求**: Axios (封装了拦截器，统一处理 token 与错误)

### 后端 (Backend)
*   **运行环境**: Node.js
*   **Web 框架**: Express.js
*   **数据库**: PostgreSQL (稳定可靠的关系型数据库)
*   **ORM/查询**: 原生 SQL 与 `pg` 库结合，保证查询性能优化的灵活性
*   **身份认证**: JWT (JSON Web Token) + Bcrypt 密码加密
*   **安全防护**: Helmet (安全头), Rate Limit (接口限流), CORS (跨域资源共享)
*   **文件存储**: Multer (处理文件上传)

---

## 📂 目录结构说明

```
talentpulse-admin/
├── Front_End/                  # 前端工程
│   ├── src/
│   │   ├── assets/             # 静态资源 (图片、样式)
│   │   ├── components/         # 公共组件
│   │   ├── modules/            # 业务模块 (按功能拆分)
│   │   │   ├── admin/          # 管理员模块
│   │   │   ├── auth/           # 认证模块 (登录/注册)
│   │   │   ├── candidate/      # 求职者模块
│   │   │   ├── recruiter/      # 招聘者模块
│   │   │   └── message/        # 消息中心模块
│   │   ├── services/           # API 请求服务
│   │   └── utils/              # 工具函数
│   ├── vite.config.ts          # Vite 配置
│   └── package.json            # 前端依赖
│
├── backend/                    # 后端工程
│   ├── src/
│   │   ├── config/             # 数据库与系统配置
│   │   ├── controllers/        # 控制器 (处理业务逻辑)
│   │   ├── routes/             # 路由定义
│   │   ├── middleware/         # 中间件 (认证、日志、错误处理)
│   │   └── utils/              # 后端工具类
│   ├── server.js               # 服务入口文件
│   └── package.json            # 后端依赖
│
├── Talent.sql                  # 数据库结构 SQL 脚本
├── add-real-data.js            # 数据填充脚本
└── README.md                   # 项目说明文档
```

---

## 🚦 快速开始指南

### 环境要求
*   **Node.js**: >= 16.0.0
*   **PostgreSQL**: >= 13.0
*   **npm** 或 **pnpm**

### 1. 数据库配置
首先创建一个新的 PostgreSQL 数据库，并导入表结构。

```bash
# 登录 PostgreSQL
psql -U postgres

# 创建数据库
CREATE DATABASE talentpulse;

# 退出并导入 SQL (在项目根目录下执行)
psql -U postgres -d talentpulse -f Talent.sql
```

### 2. 启动后端服务

```bash
cd backend

# 安装依赖
npm install

# 配置环境变量 (可选，如果 server.js 中已硬编码则跳过，建议创建 .env)
# DATABASE_URL=postgres://user:pass@localhost:5432/talentpulse
# JWT_SECRET=your_jwt_secret

# 启动开发服务 (默认端口 3001)
npm run dev
```

### 3. 启动前端应用

```bash
cd Front_End

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端启动后，访问地址通常为：
*   **求职者/招聘者端**: `http://localhost:3000`
*   **管理员端**: `http://localhost:3002` (如果在 `package.json` 中配置了单独的脚本) 或者通过路径区分。

---

## 🤝 贡献与反馈

欢迎提交 Issue 或 Pull Request 来改进 TalentPulse！

1.  Fork 本仓库
2.  新建 Feat_xxx 分支
3.  提交代码
4.  新建 Pull Request

## 📄 开源协议

MIT License
